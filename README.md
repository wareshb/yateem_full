# نظام يريم لإدارة الأيتام - الإصدار المحدث

نظام شامل ومتكامل لإدارة بيانات الأيتام، الجهات الكافلة، جهات التسويق، والمرفقات.

## 🌟 الميزات الجديدة

### قاعدة البيانات الموسعة
- **11 جدول** مع علاقات معقدة
- أكثر من **50 حقل** لبيانات اليتيم الشاملة
- دعم الأجداد المتعددة (آباء، أمهات، معيلين)
- إدارة الإخوة والأخوات
- معلومات السكن التفصيلية

### الجهات الكافلة والتسويق
- إدارة الجهات الكافلة (Organizations)
- نظام تسويق الأيتام للجهات
- **تحويل تلقائي** من جهة تسويق → جهة كافلة
- ربط أيتام متعددين بجهة واحدة

### المرفقات
- 9 أنواع من المرفقات المدعومة
- تخزين منظم حسب معرف اليتيم
- دعم الصور (JPG, PNG) و PDF
- حد أقصى 10MB لكل ملف

### التقارير المتقدمة
- تقارير شاملة مع فلترة متقدمة
- تقارير الأمهات
- الأيتام غير المكفولين/المسوقين
- تقارير حسب الفئة العمرية

## 📁 هيكل المشروع

```
yareem_pha/
├── backend/
│   ├── db/
│   │   └── schema_new.sql        # قاعدة البيانات الجديدة ⭐
│   ├── src/
│   │   ├── routes/
│   │   │   ├── orphans.js        # محدث بالكامل ⭐
│   │   │   ├── mothers.js        # جديد ⭐
│   │   │   ├── fathers.js        # جديد ⭐
│   │   │   ├── siblings.js       # جديد ⭐
│   │   │   ├── sponsor-organizations.js  # جديد ⭐
│   │   │   ├── marketing-organizations.js # جديد ⭐
│   │   │   ├── attachments.js    # جديد ⭐
│   │   │   └── reports.js        # محدث بالكامل ⭐
│   │   ├── seed_new.js           # بيانات تجريبية جديدة ⭐
│   │   └── server.js             # محدث ⭐
│   └── uploads/                  # تخزين المرفقات
│
└── frontend/
    └── src/
        ├── pages/
        │   ├── SponsorOrganizations.jsx   # جديد ⭐
        │   ├── MarketingOrganizations.jsx # جديد ⭐
        │   └── Orphans.jsx                # (سيتم تحديثه)
        └── App.jsx                         # محدث ⭐
```

## 🚀 البدء السريع

### 1. المتطلبات الأساسية
- Node.js (v16+)
- MySQL (v8+)
- npm

### 2. إعداد قاعدة البيانات

```bash
# تسجيل الدخول لـ MySQL
mysql -u root -p

# إنشاء قاعدة البيانات الجديدة
source backend/db/schema_new.sql

# التحقق من الجداول (يجب أن ترى 11 جدول)
USE yateem_pha;
SHOW TABLES;
```

### 3. إعداد Backend

```bash
cd backend
npm install

# إعداد متغيرات البيئة
cp .env.example .env
# قم بتعديل .env وأضف معلومات MySQL

# إضافة البيانات التجريبية
node src/seed_new.js

# تشغيل السيرفر
npm run dev
```

السيرفر سيعمل على: `http://localhost:4000`

### 4. إعداد Frontend

```bash
cd frontend
npm install
npm run dev
```

التطبيق سيعمل على: `http://localhost:5173`

### 5. تسجيل الدخول

- **البريد**: `admin@example.com`
- **كلمة المرور**: `admin123`

## 📊 API Endpoints الجديدة

### الأمهات
```
GET    /api/mothers              - قائمة الأمهات
POST   /api/mothers              - إضافة أم
GET    /api/mothers/:id          - تفاصيل أم
GET    /api/mothers/:id/orphans  - أيتام أم محددة
PATCH  /api/mothers/:id          - تحديث
DELETE /api/mothers/:id          - حذف
```

### الآباء
```
POST   /api/fathers           - إضافة بيانات أب
GET    /api/fathers/:id       - تفاصيل
PATCH  /api/fathers/:id       - تحديث
```

### الإخوة
```
POST   /api/siblings                      - إضافة أخ/أخت
GET    /api/orphans/:orphanId/siblings   - قائمة إخوة يتيم
PATCH  /api/siblings/:id                 - تحديث
DELETE /api/siblings/:id                 - حذف
```

### الجهات الكافلة
```
GET    /api/sponsor-organizations              - قائمة الجهات
POST   /api/sponsor-organizations              - إضافة جهة
GET    /api/sponsor-organizations/:id          - تفاصيل
POST   /api/sponsor-organizations/:id/orphans  - ربط أيتام
GET    /api/sponsor-organizations/:id/orphans  - الأيتام المكفولين
DELETE /api/sponsor-organizations/:id/orphans/:orphanId - إلغاء كفالة
```

### جهات التسويق
```
GET    /api/marketing-organizations                      - قائمة الجهات
POST   /api/marketing-organizations                      - إضافة جهة
POST   /api/marketing-organizations/:id/orphans          - تسويق أيتام
GET    /api/marketing-organizations/:id/orphans          - الأيتام المسوقين
POST   /api/marketing-organizations/:id/convert-to-sponsor - تحويل لجهة كافلة ⭐
```

### المرفقات
```
POST   /api/orphans/:orphanId/attachments  - رفع مرفق
GET    /api/orphans/:orphanId/attachments  - قائمة المرفقات
GET    /api/attachments/:id/download       - تحميل مرفق
DELETE /api/attachments/:id                - حذف مرفق
```

### التقارير
```
GET /api/reports/all-orphans     - تقرير شامل (مع فلاتر)
GET /api/reports/mothers         - تقرير الأمهات
GET /api/reports/by-sponsor      - حسب الجهة الكافلة
GET /api/reports/by-age-group    - حسب الفئة العمرية
GET /api/reports/unsponsored     - الأيتام غير المكفولين
GET /api/reports/unmarked        - الأيتام غير المسوقين
GET /api/reports/summary         - ملخص Dashboard
```

## 🎯 أمثلة الاستخدام

### إضافة يتيم كامل (مع البيانات المرتبطة)

```javascript
POST /api/orphans

{
  // بيانات اليتيم
  "orphan_id": "YT-2025-004",
  "full_name": "خالد أحمد محمد",
  "date_of_birth": "2015-05-20",
  "gender": "male",
  "nationality": "يمني",
  
  // بيانات الأب
  "father_data": {
    "full_name": "أحمد محمد",
    "date_of_death": "2020-03-15",
    "cause_of_death": "حادث"
  },
  
  // بيانات الأم
  "mother_data": {
    "full_name": "فاطمة علي",
    "phone_1": "777123456",
    "is_custodian": true
  },
  
  "mother_is_custodian": true,
  
  // بيانات السكن
  "residence_data": {
    "country": "اليمن",
    "province": "صنعاء",
    "district": "حدة"
  },
  
  // الصحة والتعليم
  "health_condition": "سليم",
  "is_studying": true,
  "grade_level": "الثالث الابتدائي",
  "school_name": "مدرسة النور",
  
  // حفظ القرآن
  "memorizes_quran": true,
  "quran_center_name": "مركز الإيمان",
  "quran_parts_memorized": 2.0,
  
  // الإخوة
  "siblings": [
    {
      "full_name": "محمد أحمد محمد",
      "date_of_birth": "2017-08-10",
      "gender": "male",
      "grade_level": "الأول الابتدائي"
    }
  ]
}
```

### تحويل جهة تسويق لجهة كافلة

```javascript
POST /api/marketing-organizations/:id/convert-to-sponsor

{
  "sponsorship_type": "نقدية",
  "start_date": "2025-01-01",
  "sponsored_orphan_ids": [1, 3, 5]  // الأيتام المكفولين
}
```

### فلترة التقارير

```
GET /api/reports/all-orphans?age_min=10&age_max=15&is_sponsored=false&province=صنعاء&sort_by=full_name&sort_order=ASC
```

## 📝 أنواع المرفقات المدعومة

1. `photo_4x6` - صورة شخصية 4×6
2. `photo_full` - صورة شخصية كاملة
3. `birth_certificate` - شهادة الميلاد
4. `father_death_certificate` - شهادة وفاة الأب
5. `mother_or_guardian_id` - بطاقة الأم أو المعيل
6. `latest_academic_certificate` - آخر شهادة دراسية
7. `inheritance_restriction` - حصر الوراثة
8. `appointment_letter` - ورقة التنصيب
9. `medical_report` - تقرير طبي

## 🔧 التقنيات المستخدمة

### Backend
- **Express.js** v5.2.1 - إطار العمل
- **MySQL2** v3.15.3 - قاعدة البيانات
- **Multer** v2.0.2 - رفع الملفات
- **JWT** - المصادقة
- **bcryptjs** - تشفير كلمات المرور

### Frontend
- **React** v19.2.0 - المكتبة الأساسية
- **Vite** v7.2.4 - أداة البناء
- **React Router** v7.10.0 - التوجيه
- **Axios** - الاتصال بـ API

## 📖 إرشادات التطوير

### إضافة endpoint جديد
1. أنشئ route file في `backend/src/routes/`
2. استورد في `server.js`
3. أضف `app.use('/api/...')` 

### إضافة صفحة frontend جديدة
1. أنشئ component في `frontend/src/pages/`
2. استورد في `App.jsx`
3. أضف route في `<Routes>`
4. أضف في `navItems` للقائمة الجانبية

## 🐛 المشاكل الشائعة

**خطأ الاتصال بقاعدة البيانات**
- تحقق من بيانات الاتصال في `.env`
- تأكد من تشغيل MySQL
- تأكد من إنشاء قاعدة البيانات `yateem_pha`

**خطأ رفع الملفات**
- تأكد من وجود مجلد `uploads`
- تحقق من أن حجم الملف أقل من 10MB
- تأكد من نوع الملف (JPG, PNG, PDF فقط)

**خطأ CORS**
- تأكد من تشغيل Backend على المنفذ 4000
- تأكد من تفعيل CORS في `server.js`

## 📄 الترخيص

هذا المشروع مفتوح المصدر للأغراض التعليمية والخيرية.

## 👥 المساهمة

نرحب بالمساهمات! يرجى فتح issue أو pull request.

---

**تم بناؤه بـ ❤️ لخدمة الأيتام**# yateem_full
